STEPS:

1. build API Server
    -> dao, dtos, services
2. build APIs
3. process incoming blocks

4. get historical price data
5. build APIs around historical data

MODEL
1. first/last input/output date (dormancy)
2. nr of unspent outputs

APIS
1. Build API Server
   - getTopAddresses (max 200)
   - getMetaAddresses
   - get addresses larger than x

SERVICES
1. process incoming blocks and update the db

OTHER
1. Sort out 35.5K addresses (pull addresses from addresses.txt to the DB)